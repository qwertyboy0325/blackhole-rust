[package]
name = "relativity-trace"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
description = "Thin-disk termination, ray outcomes, and CPU outcome-map tracing (no radiometry)"

[lints]
workspace = true

[dependencies]
relativity-core = { path = "../relativity-core" }
relativity-integrate = { path = "../relativity-integrate" }
thiserror = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
rayon = "1.10"

[dev-dependencies]
approx = "0.5"
