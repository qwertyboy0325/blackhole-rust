[package]
name = "relativity-render"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
description = "Deterministic diagnostic celestial texture sampling and lensed RGB frames (no radiometry)"

[lints]
workspace = true

[dependencies]
relativity-core = { path = "../relativity-core" }
relativity-trace = { path = "../relativity-trace" }
thiserror = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

[dev-dependencies]
approx = "0.5"
