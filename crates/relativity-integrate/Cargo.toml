[package]
name = "relativity-integrate"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
description = "CPU f64 DOP853 geodesic integrator adapter over ivp (no I/O, no rendering)"

[lints]
workspace = true

[dependencies]
relativity-core = { path = "../relativity-core" }
ivp = "=0.6.0"
thiserror = "2"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
approx = "0.5"
serde_json = "1"
