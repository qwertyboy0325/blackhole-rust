[package]
name = "gate-1b0-contract"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
description = "Gate 1B0 experimental contract types and analytic systems (not production)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
hex = "0.4"

[lints]
workspace = true
